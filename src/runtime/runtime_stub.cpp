#include "rpc/runtime/runtime.h"

// 文件用途：
// runtime 模块初始化占位实现。
// 后续可在此初始化调度器、IO 管理器和 Hook 子系统。

namespace rpc::runtime {

void init_runtime() {
    // 当前阶段无实际初始化逻辑，保留空实现用于打通启动链路。
}

}  // namespace rpc::runtime
